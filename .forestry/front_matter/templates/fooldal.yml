---
label: fooldal
hide_body: true
fields:
- name: landing
  type: blocks
  label: landing
  template_types:
  - landing
  config:
    min: 
    max: 
- name: referenciak
  type: blocks
  label: referenciak
  template_types:
  - referenciak
  config:
    min: 
    max: 
- name: rolunk
  type: blocks
  label: rolunk
  template_types:
  - rolunk
  config:
    min: 
    max: 
- name: termekeink
  type: blocks
  label: termekeink
  template_types:
  - termekeink
  config:
    min: 
    max: 
