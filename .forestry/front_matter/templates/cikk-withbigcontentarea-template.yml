---
label: cikk
hide_body: false
display_field: id
fields:
- name: cim
  type: text
  config:
    required: true
  label: cim
- name: datum
  type: datetime
  label: datum
  description: ''
  config:
    required: false
    date_format: YYYY/MM/DD
    time_format: YYYY/MM/DD
    display_utc: true
    export_format: YYYY/MM/DD
- name: kivonat
  type: textarea
  default: ''
  config:
    required: true
    wysiwyg: true
    schema:
      format: markdown
  label: kivonat
- name: boritokep
  type: file
  config:
    maxSize: 64
  label: boritokep
- name: seo
  type: blocks
  label: seo
  template_types:
  - seo
  config:
    min: 
    max: 
pages:
- cms/cikkek/test-cikk.md
