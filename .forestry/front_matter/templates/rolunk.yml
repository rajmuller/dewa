---
label: rolunk
hide_body: true
fields:
- name: title
  type: text
  config:
    required: true
  label: title
  default: Rólunk
- name: subtitle
  type: text
  config:
    required: true
  label: subtitle
- name: kep
  type: file
  config:
    maxSize: 64
  label: kep
